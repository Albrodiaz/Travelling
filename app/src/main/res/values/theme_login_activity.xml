<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.LoginActivity" parent="Theme.Travelling">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/grey_dark</item>
        <item name="colorOnPrimary">@color/grey_extra_light</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/pink_pastel</item>
        <item name="colorOnSecondary">@color/grey_dark</item>
        <!-- Customize your theme here. -->
        <item name="fontFamily">sans-serif-medium</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="materialButtonStyle">@style/LoginButtonStyle</item>
        <item name="textInputStyle">@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox</item>
    </style>

</resources>